import 'package:flutter/material.dart';

class ShoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.red,
        child: Center(
          child: Container(child: Text("Shop Page")),
        ),
      ),
    );
  }
}
